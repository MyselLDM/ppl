var char first_char = 'a';
var char second_char = 'b';
var string first_string = "hello";
var string second_string = "Hello World";
var int first_int = 32;
var third_int = 34 * 32 ^ 2 + 32 - 2 + first_int;
var s = '432';

// This is my Comment

$4 @233;;;

for (var i = 0; i <= 32; < i++) {
  first_int--;
}

if (first_int == 32) {
  first_int = 2;
} else {
  first_int = 3;
}