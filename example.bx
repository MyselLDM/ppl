var char first_char = 'a';
var char second_char = 'b';
var int first_int = 32;
var s = '432';

// This is my Comment

$4 @233;;;

for (var i = 0; i <= 32; < i++) {
  first_int--;
}

if (first_int EQUALS 32) {
  first_int = 2;
} else {
  first_int = 3;
}