var char first_char = 'a';
var char second_char = 'b';
var int first_int = 32;

// This is my Comment

/// This should be the multiline comment
Test 123 Hello !var x = 54
    ///

    var float first_float = 54.23 + 32.43;
var int second_int = 324 * 32 + 44 - 66 + first_int;

$4 @233;
;
;
!

    for (var i = 0; i <= 32; < i++) {
  first_int--;
}

if (first_int EQUALS 32) {
  first_int = 2;
} else {
  first_int = 3;
}
END

    var s = '432';
